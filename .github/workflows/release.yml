name: Release

on:
  push:
    branches: [production]

jobs:
  bump-version:
    runs-on: ubuntu-latest
    outputs:
      new_tag: ${{ steps.tag.outputs.new_tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Bump version and push tag
        id: tag
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: patch
          tag_prefix: ""
          release_branches: "production"

  build:
    needs: bump-version
    strategy:
      fail-fast: false
      matrix:
        include:
          # ESP32: FemtoRuby
          - target: esp32
            vm_name: femtoruby
            vm_flag: mrubyc
            artifact_suffix: esp32-femtoruby
            sdkconfig_defaults: sdkconfig.defaults
          # ESP32-C3: FemtoRuby
          - target: esp32c3
            vm_name: femtoruby
            vm_flag: mrubyc
            artifact_suffix: esp32c3-femtoruby
            sdkconfig_defaults: sdkconfig.defaults
          # ESP32-C6: PicoRuby
          - target: esp32c6
            vm_flag: mruby
            artifact_suffix: esp32c6-picoruby
            sdkconfig_defaults: sdkconfig.defaults
          - target: esp32c6
            vm_flag: mruby
            artifact_suffix: esp32c6-usb_console-picoruby
            sdkconfig_defaults: "sdkconfig.defaults;sdkconfigs/usb_console"
          # ESP32-H2: FemtoRuby
          - target: esp32h2
            vm_flag: mrubyc
            artifact_suffix: esp32h2-femtoruby
            sdkconfig_defaults: sdkconfig.defaults
          - target: esp32h2
            vm_flag: mrubyc
            artifact_suffix: esp32h2-usb_console-femtoruby
            sdkconfig_defaults: "sdkconfig.defaults;sdkconfigs/usb_console"
          # ESP32-P4: PicoRuby
          - target: esp32p4
            vm_flag: mruby
            artifact_suffix: esp32p4-picoruby
            sdkconfig_defaults: sdkconfig.defaults
          - target: esp32p4
            vm_flag: mruby
            artifact_suffix: esp32p4-usb_console-picoruby
            sdkconfig_defaults: "sdkconfig.defaults;sdkconfigs/usb_console"
          # ESP32-S3: FemtoRuby and PicoRuby
          - target: esp32s3
            vm_name: femtoruby
            vm_flag: mrubyc
            artifact_suffix: esp32s3-femtoruby
            sdkconfig_defaults: sdkconfig.defaults
          - target: esp32s3
            vm_name: picoruby
            vm_flag: mruby
            artifact_suffix: esp32s3-picoruby
            sdkconfig_defaults: sdkconfig.defaults
          - target: esp32s3
            vm_name: femtoruby
            vm_flag: mrubyc
            artifact_suffix: esp32s3-usb_console-femtoruby
            sdkconfig_defaults: "sdkconfig.defaults;sdkconfigs/usb_console"
          - target: esp32s3
            vm_name: picoruby
            vm_flag: mruby
            artifact_suffix: esp32s3-usb_console-picoruby
            sdkconfig_defaults: "sdkconfig.defaults;sdkconfigs/usb_console"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
      - name: Bundle install
        run: bundle install
        working-directory: ./components/picoruby-esp32/picoruby
      - name: Build PicoRuby
        run: rake
        working-directory: ./components/picoruby-esp32/picoruby
      - name: Build firmware
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.5.1
          target: ${{ matrix.target }}
          path: '.'
          command: SDKCONFIG_DEFAULTS="${{ matrix.sdkconfig_defaults }}" idf.py build -DPICORB_VM=${{ matrix.vm_flag }}
      - name: Rename artifact
        run: cp build/R2P2-ESP32.bin R2P2-ESP32-${{ matrix.artifact_suffix }}.bin
      - uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.artifact_suffix }}
          path: R2P2-ESP32-${{ matrix.artifact_suffix }}.bin

  release:
    needs: [bump-version, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: firmware-*
          merge-multiple: true
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.bump-version.outputs.new_tag }}
          name: Release ${{ needs.bump-version.outputs.new_tag }}
          files: "*.bin"
